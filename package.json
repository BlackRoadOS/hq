{
  "name": "blackroad-hq",
  "version": "1.0.0",
  "private": true,
  "description": "BlackRoad HQ — Pixel world for sovereign AI agents",
  "scripts": {
    "dev": "npx serve public -l 4200",
    "build": "cp -r src public/src && echo 'Built.'",
    "deploy": "npx wrangler pages deploy public --project-name=hq-blackroad"
  },
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.4.2"
  }
}
